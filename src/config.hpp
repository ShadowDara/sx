#pragma once

#include <vector>
#include <string>

// Config Struct for SX
struct Config {
    bool NewStartMessage = false;
};

// Reserved Commads
std::vector<std::string> ReservedCommands = {"", "", ""};
